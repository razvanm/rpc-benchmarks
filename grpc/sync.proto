syntax = "proto3";

package sync;

message Payload {
    bytes payload = 1;
}

message Void {}

service Sync {
    rpc Sync(Payload) returns (Void) {}
    rpc SyncStream(stream Payload) returns (Void) {}
}