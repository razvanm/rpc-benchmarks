package sync

import (
	"v.io/v23/security/access"
)

type Sync interface {
  // Sync always returns nil.
  Sync(Payload []byte) error        {access.Read}
  // SyncStream always returns nil.
  SyncStream() stream<[]byte> error {access.Read}
}