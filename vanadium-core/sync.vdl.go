// This file was auto-generated by the vanadium vdl tool.
// Package: sync

package sync

import (
	"io"
	"v.io/v23"
	"v.io/v23/context"
	"v.io/v23/rpc"
	"v.io/v23/security/access"
	"v.io/v23/vdl"
)

var _ = __VDLInit() // Must be first; see __VDLInit comments for details.

//////////////////////////////////////////////////
// Interface definitions

// SyncClientMethods is the client interface
// containing Sync methods.
type SyncClientMethods interface {
	// Sync always returns nil.
	Sync(_ *context.T, Payload []byte, _ ...rpc.CallOpt) error
	// SyncStream always returns nil.
	SyncStream(*context.T, ...rpc.CallOpt) (SyncSyncStreamClientCall, error)
}

// SyncClientStub adds universal methods to SyncClientMethods.
type SyncClientStub interface {
	SyncClientMethods
	rpc.UniversalServiceMethods
}

// SyncClient returns a client stub for Sync.
func SyncClient(name string) SyncClientStub {
	return implSyncClientStub{name}
}

type implSyncClientStub struct {
	name string
}

func (c implSyncClientStub) Sync(ctx *context.T, i0 []byte, opts ...rpc.CallOpt) (err error) {
	err = v23.GetClient(ctx).Call(ctx, c.name, "Sync", []interface{}{i0}, nil, opts...)
	return
}

func (c implSyncClientStub) SyncStream(ctx *context.T, opts ...rpc.CallOpt) (ocall SyncSyncStreamClientCall, err error) {
	var call rpc.ClientCall
	if call, err = v23.GetClient(ctx).StartCall(ctx, c.name, "SyncStream", nil, opts...); err != nil {
		return
	}
	ocall = &implSyncSyncStreamClientCall{ClientCall: call}
	return
}

// SyncSyncStreamClientStream is the client stream for Sync.SyncStream.
type SyncSyncStreamClientStream interface {
	// SendStream returns the send side of the Sync.SyncStream client stream.
	SendStream() interface {
		// Send places the item onto the output stream.  Returns errors
		// encountered while sending, or if Send is called after Close or
		// the stream has been canceled.  Blocks if there is no buffer
		// space; will unblock when buffer space is available or after
		// the stream has been canceled.
		Send(item []byte) error
		// Close indicates to the server that no more items will be sent;
		// server Recv calls will receive io.EOF after all sent items.
		// This is an optional call - e.g. a client might call Close if it
		// needs to continue receiving items from the server after it's
		// done sending.  Returns errors encountered while closing, or if
		// Close is called after the stream has been canceled.  Like Send,
		// blocks if there is no buffer space available.
		Close() error
	}
}

// SyncSyncStreamClientCall represents the call returned from Sync.SyncStream.
type SyncSyncStreamClientCall interface {
	SyncSyncStreamClientStream
	// Finish performs the equivalent of SendStream().Close, then blocks until
	// the server is done, and returns the positional return values for the call.
	//
	// Finish returns immediately if the call has been canceled; depending on the
	// timing the output could either be an error signaling cancelation, or the
	// valid positional return values from the server.
	//
	// Calling Finish is mandatory for releasing stream resources, unless the call
	// has been canceled or any of the other methods return an error.  Finish should
	// be called at most once.
	Finish() error
}

type implSyncSyncStreamClientCall struct {
	rpc.ClientCall
}

func (c *implSyncSyncStreamClientCall) SendStream() interface {
	Send(item []byte) error
	Close() error
} {
	return implSyncSyncStreamClientCallSend{c}
}

type implSyncSyncStreamClientCallSend struct {
	c *implSyncSyncStreamClientCall
}

func (c implSyncSyncStreamClientCallSend) Send(item []byte) error {
	return c.c.Send(item)
}
func (c implSyncSyncStreamClientCallSend) Close() error {
	return c.c.CloseSend()
}
func (c *implSyncSyncStreamClientCall) Finish() (err error) {
	err = c.ClientCall.Finish()
	return
}

// SyncServerMethods is the interface a server writer
// implements for Sync.
type SyncServerMethods interface {
	// Sync always returns nil.
	Sync(_ *context.T, _ rpc.ServerCall, Payload []byte) error
	// SyncStream always returns nil.
	SyncStream(*context.T, SyncSyncStreamServerCall) error
}

// SyncServerStubMethods is the server interface containing
// Sync methods, as expected by rpc.Server.
// The only difference between this interface and SyncServerMethods
// is the streaming methods.
type SyncServerStubMethods interface {
	// Sync always returns nil.
	Sync(_ *context.T, _ rpc.ServerCall, Payload []byte) error
	// SyncStream always returns nil.
	SyncStream(*context.T, *SyncSyncStreamServerCallStub) error
}

// SyncServerStub adds universal methods to SyncServerStubMethods.
type SyncServerStub interface {
	SyncServerStubMethods
	// Describe the Sync interfaces.
	Describe__() []rpc.InterfaceDesc
}

// SyncServer returns a server stub for Sync.
// It converts an implementation of SyncServerMethods into
// an object that may be used by rpc.Server.
func SyncServer(impl SyncServerMethods) SyncServerStub {
	stub := implSyncServerStub{
		impl: impl,
	}
	// Initialize GlobState; always check the stub itself first, to handle the
	// case where the user has the Glob method defined in their VDL source.
	if gs := rpc.NewGlobState(stub); gs != nil {
		stub.gs = gs
	} else if gs := rpc.NewGlobState(impl); gs != nil {
		stub.gs = gs
	}
	return stub
}

type implSyncServerStub struct {
	impl SyncServerMethods
	gs   *rpc.GlobState
}

func (s implSyncServerStub) Sync(ctx *context.T, call rpc.ServerCall, i0 []byte) error {
	return s.impl.Sync(ctx, call, i0)
}

func (s implSyncServerStub) SyncStream(ctx *context.T, call *SyncSyncStreamServerCallStub) error {
	return s.impl.SyncStream(ctx, call)
}

func (s implSyncServerStub) Globber() *rpc.GlobState {
	return s.gs
}

func (s implSyncServerStub) Describe__() []rpc.InterfaceDesc {
	return []rpc.InterfaceDesc{SyncDesc}
}

// SyncDesc describes the Sync interface.
var SyncDesc rpc.InterfaceDesc = descSync

// descSync hides the desc to keep godoc clean.
var descSync = rpc.InterfaceDesc{
	Name:    "Sync",
	PkgPath: "github.com/razvanm/rpc-benchmarks/vanadium-core",
	Methods: []rpc.MethodDesc{
		{
			Name: "Sync",
			Doc:  "// Sync always returns nil.",
			InArgs: []rpc.ArgDesc{
				{"Payload", ``}, // []byte
			},
			Tags: []*vdl.Value{vdl.ValueOf(access.Tag("Read"))},
		},
		{
			Name: "SyncStream",
			Doc:  "// SyncStream always returns nil.",
			Tags: []*vdl.Value{vdl.ValueOf(access.Tag("Read"))},
		},
	},
}

// SyncSyncStreamServerStream is the server stream for Sync.SyncStream.
type SyncSyncStreamServerStream interface {
	// RecvStream returns the receiver side of the Sync.SyncStream server stream.
	RecvStream() interface {
		// Advance stages an item so that it may be retrieved via Value.  Returns
		// true iff there is an item to retrieve.  Advance must be called before
		// Value is called.  May block if an item is not available.
		Advance() bool
		// Value returns the item that was staged by Advance.  May panic if Advance
		// returned false or was not called.  Never blocks.
		Value() []byte
		// Err returns any error encountered by Advance.  Never blocks.
		Err() error
	}
}

// SyncSyncStreamServerCall represents the context passed to Sync.SyncStream.
type SyncSyncStreamServerCall interface {
	rpc.ServerCall
	SyncSyncStreamServerStream
}

// SyncSyncStreamServerCallStub is a wrapper that converts rpc.StreamServerCall into
// a typesafe stub that implements SyncSyncStreamServerCall.
type SyncSyncStreamServerCallStub struct {
	rpc.StreamServerCall
	valRecv []byte
	errRecv error
}

// Init initializes SyncSyncStreamServerCallStub from rpc.StreamServerCall.
func (s *SyncSyncStreamServerCallStub) Init(call rpc.StreamServerCall) {
	s.StreamServerCall = call
}

// RecvStream returns the receiver side of the Sync.SyncStream server stream.
func (s *SyncSyncStreamServerCallStub) RecvStream() interface {
	Advance() bool
	Value() []byte
	Err() error
} {
	return implSyncSyncStreamServerCallRecv{s}
}

type implSyncSyncStreamServerCallRecv struct {
	s *SyncSyncStreamServerCallStub
}

func (s implSyncSyncStreamServerCallRecv) Advance() bool {
	s.s.errRecv = s.s.Recv(&s.s.valRecv)
	return s.s.errRecv == nil
}
func (s implSyncSyncStreamServerCallRecv) Value() []byte {
	return s.s.valRecv
}
func (s implSyncSyncStreamServerCallRecv) Err() error {
	if s.s.errRecv == io.EOF {
		return nil
	}
	return s.s.errRecv
}

var __VDLInitCalled bool

// __VDLInit performs vdl initialization.  It is safe to call multiple times.
// If you have an init ordering issue, just insert the following line verbatim
// into your source files in this package, right after the "package foo" clause:
//
//    var _ = __VDLInit()
//
// The purpose of this function is to ensure that vdl initialization occurs in
// the right order, and very early in the init sequence.  In particular, vdl
// registration and package variable initialization needs to occur before
// functions like vdl.TypeOf will work properly.
//
// This function returns a dummy value, so that it can be used to initialize the
// first var in the file, to take advantage of Go's defined init order.
func __VDLInit() struct{} {
	if __VDLInitCalled {
		return struct{}{}
	}
	__VDLInitCalled = true

	return struct{}{}
}
